#!-*- coding:utf8 -*-
from django.conf.urls import include, url
from general import views as general_views


urlpatterns = [
]

# urlpatterns = patterns('general.views',
    # Example:
    # (r'^rcm5/', include('rcm5.foo.urls')),

    # Uncomment this for admin:
    # (r'^error/(?P<type>\w+)/$', 'error'),
    # (r'^addunitno/$', 'addUnitNo'),
    # (r'^findnamebyno/$', 'findNameByNo'),
    # (r'^getcompanybyno/(?P<no>[0-9]{8})/$', 'getCompanyByNo'),
    # (r'^ajax/$', 'rJSON'),
# )

